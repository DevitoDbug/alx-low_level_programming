#include "main.h"

/**
 *print_square - prints a squar
 *@size: print int
 * Return: Always 0.
 */

void print_square(int size)
{
	int i;
	int j;

	if (n > 0)
	{
		for (i = 0; i < n; i++)
		{
			for (j = 0; j < n; j++)
			{
				_putchar (35);
			}
			_putchar ('\n');
		}
	}
	else
			_putchar ('\n');
}
